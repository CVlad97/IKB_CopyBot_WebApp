{
  "name": "ikb-copybot-webapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html styles.css script.js dist/",
    "preview": "python3 -m http.server 4176"
  }
}
